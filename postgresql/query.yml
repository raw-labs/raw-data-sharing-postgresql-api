raw: "0.10"
endpoint: GET
metadata:
  title: Simple greeting query.
  description: Retrieves a greeting message 'Hello, World!' as a result of the SQL query. <BR/><BR/>This is a placeholder SQL query. You can replace this query with any valid SQL query against your PostgreSQL database.<BR/><BR/>For example:<BR/><code>SELECT * FROM mysql.&lt;your_table&gt;</code><BR/><BR/>If you introduce parameters, be sure to define them using RAW's parameter syntax.
  tags:
    - data_sharing
    - postgresql
code: sql
codeFile: query.sql
format: json
security:
  public: true
computeClass: normal
enabled: true
